\chapter{Sequential Production Economies}
\section*{Timeline}
Firms buy at time 0 on the spot market and sell at time 1 on the state-contingent markets.

\section{Takeaways}
\begin{itemize}
    \item Arrow-Debreu equilibrium 
    \item Financial Market equilibrium 
    \item Minkowski's rational price conjecture and Dreze conjecture 
    \item The most important proof: (L = 1, H = 1, J = 0) \\
    \underline{Competitive equilibria of FME with production under Rational Conjectures are Constrained P.E.}
    \item Corporate finance (e.g., stocks vs. bonds) 
\end{itemize}

\section*{Notations}
\begin{itemize}
    \item $\delta^{ih}$: endowment of firms' shares 
    \item $\phi$: prices 
    \item $\theta$: quantity of equity trade
\end{itemize}

\clearpage 
\section{Arrow-Debreu Equilibrium (with Production)}
\noindent \textbf{Definition.} \\
An Arrow-Debreu Equilibrium is $(x, y, \phi) 
\in \mathbb{R}^{L(S+1)I}_{+} \times \Upsilon \times \mathbb{R}^{L(S+1)}_{++}$
(N.B.: $y = [y_h], \text{ with } y_h = (-k^h, f^h(k^h))$.) such that 
\begin{enumerate}
    \item Given $\phi$, each individual solves the utility maximization pb:
    \begin{align*}
        \max_{x^i_0, x^i_s} \quad & u^i(x^i_0) + \sum_s \text{Prob}_s u^i(x^i_s) \\
        \text{subject to: } \quad & \phi_0 (x^i_0 - w^i_0) + \sum_s \phi_s (x^i_s - w^i_s)  + \sum_h \delta^{ih} k_h = 0 
    \end{align*}
    \item Each firm solves the following pb:
    \begin{align*}
        \max_{k^h} \quad & - \phi_0 k^h + \textcolor{blue}{\sum_s \phi_s} f^h(k_h;s)
    \end{align*}
    \item Market clears%
    \footnote{Since $y^h = [-k^h, f^h(k^h)]$, this condition is the same as 
    \begin{align*}
        \sum_i x_0^i & \leq \sum_i w_0^i - \sum_h k^h \\
        \sum_i x_s^i & \leq \sum_i w_s^i + \sum_h f^h(k^h;s)
    \end{align*}
    }
    \begin{align*}
        \sum_i x^i \leq \sum_i w^i + \sum_h y^h
    \end{align*}
    where $x^i \in \mathbb{R}^{L(S+1)}_{+}$.
    \item Profits are 
    \[
    \pi^h = - \phi_0 k^h + \sum_s \phi_s f^h(k^h;s)
    \]
\end{enumerate}

\clearpage
\section{Financial Market Equilibrium (with Production)}

\begin{table}[htbp]
\centering
\begin{tabular}{|m{8cm}|m{6cm}|}
\hline
\textbf{Full Model} & \textbf{Simplified ($J = 0, L = H = 1$)} \\ \hline
\noindent \textbf{Consumer solves}
\begin{align*}
    \max_{x^i, \theta^i, z^i} u^i(x^i_0) + \sum_s \text{Prob}_s u^i(x^i_s)  \\
    \text{s.t.} \quad p_0 (x^i_0 - w^i_0) + q z^i = (-p_0 k + Q) \theta^i_0 - Q \theta^i \\
    p_s (x^i_s - w^i_s) = \theta^i p_s f^h(k;s) + a_s z^i
\end{align*}
& 
\noindent \textbf{Consumer solves}
\begin{align*}
    \max_{x^i, \theta^i, z^i} u^i(x^i_0) + \sum_s \text{Prob}_s u^i(x^i_s)  \\
    \text{s.t.} \quad  (x^i_0 - w^i_0)  = (- k + Q) \theta^i_0 - Q \theta^i \\
   (x^i_s - w^i_s) = \theta^i p_s f^h(k;s) 
\end{align*} 
\vspace{-2mm} \\ \hline
\noindent \textbf{Firm solves} 
\[
\max_{k^h} \quad - p_0 k^h + Q(k^h)
\]
& 
\noindent \textbf{Firm solves} 
\[
\max_{k^h} \quad - k^h + Q(k^h)
\]
\vspace{-2mm} \\ \hline
\noindent \textbf{Market clears} 
\begin{align*}
\sum_i x^i_0 + \sum_h k^h \leq \sum_i w^i_0 \\ 
\sum_i x^i_s \leq \sum_i w^i_s + \sum_h f^h(k^h;s) \\ 
\sum_i z^i = 0 \quad \text{ and } \quad \sum_i \theta^i = 1
\end{align*} 
& 
\noindent \textbf{Market clears} 
\begin{align*}
\sum_i x^i_0 + \sum_h k^h \leq \sum_i w^i_0 \\ 
\sum_i x^i_s \leq \sum_i w^i_s + \sum_h f^h(k^h;s) \\
\sum_i \theta^i = 1
\end{align*} 
\vspace{-2mm} \\ \hline
\noindent \textbf{Rational Conjectures} 
\begin{align*}
Q^h(k^h) = \max_i \mathbb{E}[m^i p \circ f^h(k^h)]
\end{align*} 
& 
\noindent \textbf{Rational Conjectures} 
\begin{align*}
Q^h(k^h) = \max_i \mathbb{E}[m^i p \circ f^h(k^h)]
\end{align*} 
\vspace{-2mm} \\ \hline
\noindent \textbf{Consistency in equity prices} 
\begin{align*}
Q^h = Q^h(k^h), \quad \forall h \in H
\end{align*} 
& 
\noindent \textbf{Consistency in equity prices} 
\begin{align*}
Q^h = Q^h(k^h), \quad \forall h \in H
\end{align*} 
\\ \hline
\end{tabular}
\label{table:equilibrium}
\end{table}

\clearpage
\section{Rational price conjectures}


\begin{tcolorbox}
\begin{proposition}
Define $Q^h(k^h) := \max_i \mathbb{E}[m^i p \circ f^h(k^h)]$, and 
$D^h(k^h) := \mathbb{E}[\sum_i \theta^i_h m^i p \circ f^h(k^h)]$, we have
\[
\text{FME with Rational Conjectures } (Q^h) \quad \overset{\Rightarrow}{\nLeftarrow} \quad \text{FME with Dreze Conjectures} (D^h)
\]
\end{proposition}
\noindent \textbf{Proof (Sketch):} Note that in equilibrium, 
these equity prices coincide with RC and Dreze Conjecture, namely, $Q^h({k^h}^*) = D^h({k^h}^*)$,
where ${k^h}^*$ is the equilibrium input choice of firm $h$.
However, away from equilibrium, we have 
\[
Q^h(k^h) \geq D^h(k^h)
\]
Hence, there are no profitable deviations from equilibrium under RC, (that is the $\Rightarrow$ part),
but there could be profitable deviations under Dreze (that is the $\nLeftarrow$ part).
\(\blacksquare\)    
\end{tcolorbox}


